
--First Set
1. Show users with order_amount greater than 1000

    select * from MOCK_DATA
    where order_amount > 1000

        Execution finished without errors.
        Result: 993 rows returned in 29ms

2. Show users registered after January 1st, 2025

    select * from MOCK_DATA
    where registration_date >= '02/01/2025'

        Execution finished without errors.
        Result: 962 rows returned in 37ms

3. Show users whose policy expires within the next 30 days

    select * from MOCK_DATA
    where policy_expiry = DATE('now'+'30 days')

        Execution finished without errors.
        Result: 0 rows returned in 22ms

        -- this is wrong !!! Need a bit of investigation

4. Show users with IP addresses starting with 192.

    select * from MOCK_DATA
    where ip_address like '192.%'

        Execution finished without errors.
        Result: 5 rows returned in 24ms

5. Show users who are active and from Poland

    select * from MOCK_DATA
    where active IS TRUE and country = 'Poland'

        Execution finished without errors.
        Result: 18 rows returned in 30ms




